# 🚀 Настройка Vercel для OAuth Asana

## ✅ Что было исправлено

1. **package.json** - `@vercel/node` перенесен в `dependencies`
2. **api/tsconfig.json** - настроен для ES modules
3. **vercel.json** - исправлен runtime на `nodejs20.x`
4. **api/auth/token.ts** - улучшено логирование и обработка ошибок

## 📝 Шаги для деплоя

### 1. Коммит и пуш изменений

```bash
git add .
git commit -m "fix: исправлена конфигурация Vercel serverless функций"
git push origin main
```

### 2. Настройка Environment Variables на Vercel

Перейдите в настройки проекта на Vercel:
`https://vercel.com/your-username/time-tracking-app/settings/environment-variables`

Добавьте следующие переменные для **Production, Preview и Development**:

#### Обязательные переменные:

```
ASANA_CLIENT_ID=1211555412984610
ASANA_CLIENT_SECRET=ваш_секретный_ключ_от_asana
ASANA_REDIRECT_URI=https://time-tracking-app-sigma.vercel.app/auth/callback
```

**⚠️ ВАЖНО:** 
- Используйте имена **БЕЗ** префикса `VITE_` для серверных функций
- `ASANA_CLIENT_SECRET` должен быть взят из Asana App Settings
- Все три переменные должны быть установлены для всех окружений

### 3. Проверка логов после деплоя

После деплоя проверьте логи функции:
1. Откройте Vercel Dashboard
2. Перейдите в проект → Deployments
3. Выберите последний деплой
4. Откройте вкладку "Functions"
5. Найдите `/api/auth/token` и посмотрите логи

В логах должно быть видно:
```
=== Token Exchange Function Started ===
Environment variables check:
- ASANA_CLIENT_ID: Present (1211555412...)
- ASANA_CLIENT_SECRET: Present (hidden)
- ASANA_REDIRECT_URI: https://time-tracking-app-sigma.vercel.app/auth/callback
```

### 4. Проверка в Asana App Settings

Убедитесь, что в настройках OAuth приложения Asana:
- **Redirect URL** точно совпадает: `https://time-tracking-app-sigma.vercel.app/auth/callback`
- Приложение активировано

## 🔍 Диагностика проблем

### Если видите "Missing environment variables"
- Переменные не установлены на Vercel
- Нужно добавить их через Settings → Environment Variables
- После добавления нужно сделать **Redeploy**

### Если видите ошибку от Asana API
- Проверьте, что `ASANA_CLIENT_SECRET` правильный
- Проверьте, что `ASANA_REDIRECT_URI` совпадает с настройками в Asana App

### Если функция не вызывается
- Проверьте CORS в логах
- Убедитесь, что фронтенд делает запрос на правильный URL

## 📚 Полезные ссылки

- [Vercel Environment Variables](https://vercel.com/docs/projects/environment-variables)
- [Vercel Serverless Functions](https://vercel.com/docs/functions/serverless-functions)
- [Asana OAuth Documentation](https://developers.asana.com/docs/oauth)

